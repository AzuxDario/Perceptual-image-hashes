#include "PercepturalHash.h"

namespace Pih
{
    std::vector<unsigned char> PercepturalHash::calculateHash(const std::vector<unsigned char>& image)
    {
        std::vector<unsigned char> result;
        result.reserve(size * size);
        
        return result;
    }
}